import React, { useEffect, useState } from "react";

const InputAndScore = () => {
    //this needs to hold the logic for the variable found , but the routing is done in app
}

export default InputAndScore;